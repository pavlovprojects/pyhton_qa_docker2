version: '3'
services:
  web:
    build:
      context: ../flask_site
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      FLASK_ENV: development

  tests:
    build:
      context: ../tests
      dockerfile: Dockerfile
    depends_on:
      - web
